# 当前项目仓库的路径
projectPath="~/handpy-SeniorOS/"

# 对应feature:DebugMessage 调试信息
# 此处可选择对应需要开启调试信息的模块
# 可保持原样
debugMessage={
    "Core.DataCtrl.__init__":True
}

# 常量数据 大部分情况下不需要更改
constData={
    "screenshotMethod":"fast", # 截屏模块方法
    "version":"1.0.0" # 版本号(暂时无用)
}